-- File: lua/plugins/ui.lua
-- Berisi plugin-plugin yang berhubungan dengan User Interface (UI)

return {
	-- NOTE: fidget.nvim is configured in rest.lua as a dependency for rest.nvim
	-- This avoids plugin duplication while maintaining UI organization
	
	-- You can add other UI plugins here in the future,
	-- such as statusline, icons, etc.
}
